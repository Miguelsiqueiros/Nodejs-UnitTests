// const mongodb = require("./src/data/mongo-in-memory");
// const userSchema = require("./src/data/schema/users-schema");

// // mongodb.connect().then(() => {
// //   userSchema.create({ name: "Miguel", pin: 1234 }).then((document, error) => {
// //     if (error) console.log(error);
// //     console.log(document);
// //     mongodb.closeDatabase();
// //   });
// // });

const express = require("express");

module.exports = () => {};
